<!doctype html>
<html lang="en-gb" class="{{ pageClass }}">
  {{> head}}

<body class="no-js">
{{#if home}}
  {{> header-home}}
{{else}}
  {{> header}}
{{/if}}

  <div class="wrapper">
    <main role="main">
      {{{ contents }}}
    </main>
    {{#unless home}}
      {{> aside}}
    {{/unless}}
  </div>

<script src="/assets/scripts.js"></script>
</body>
</html>
